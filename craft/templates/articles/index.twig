{% extends "_layouts/standard" %}
{% set theCategory = craft.request.getSegment(2) %}
{% block content %}
<pre>

{% set groups = craft.categories.group('articleCategory').slug(theCategory).first() %}

{% set Structure = craft.entries.section('articles').relatedTo({
	targetElement: groups
}) %}
{% paginate Structure.limit(1) as pageInfo, entries %}
{#% set entries = craft.entries.section('articles').relatedTo({
	targetElement: groups
}) %#}


</pre>
{# Page Header #}
<header class="page-header bg-blue">
	<div class="simple-container py-8 sm:py-10">
		{# Landing Page Title: #}
		<h1 class="text-white font-medium md:text-3xl sm:text-xl text-lg">{{craft.categories.slug(theCategory).first().title}}</h1>
	</div>
</header>

{# Container #}
<div class="simple-container">

	{# Article Type Selector #}
	<div class="max-w-1xs mb-6 mt-5 pt-10">
		<select data-module="choicesAsLinks" name="" id="">
			<option value="https://github.com/jshjohnson/Choices">All Types</option>
			<option value="https://github.com/TelegraphCreative/bottle-checkout-fed">Type One</option>
			<option value="https://app.asana.com/0/581664732076036/board">Type Two</option>
		</select>
	</div>

	<div class="lg:flex lg:flex-wrap mb-lg md:-mx-4">

		{# Main #}
		<section class="main-content   lg:w-2/3 md:px-4 ">

			{# Inf Scroll Wrap #}
			<div class="js-infScroll   border-t border-grey-lightest mb-14   lg:mb-18" data-module="infScroll">
				{# Article Summary #}

{# Output them #}
{% for entry in entries %}
		{# Article Summary #}
		<article class="inf-card   border-b border-grey-lightest mt-12 pb-6">
			<time class="block leading-normal mb-2 text-grey-dark text-xs">{{date.simple(entry.postDate)}}</time>
			<h1 class="mb-5 text-2md text-blue   sm:text-lg   md:text-2md   lg:text-lg">{{entry.title}}</h1>

			<div class="lg:flex lg:items-end">
				<div class="leading-normal mb-6 text-xs text-grey-dark">{{entry.body}}</div>

				{# button #}
				<a href="{{entry.url}}"class="btn --auto-w  --text lg:flex-none lg:ml-16">Read More</a>

				{# text #}
				{#
				<a class="btn --text   px-0   lg:flex-none lg:ml-16 lg:pr-2">TCEQ’s Rule Log</a>
				#}
			</div>
		</article>
{% endfor %}
			</div>


			{# Inf Scroll Pagination #}

			{% if pageInfo.nextUrl %}
				<div class="pagination hidden">
					<a class="pagination__next" href="{{ pageInfo.nextUrl }}">Next page</a>
				</div>
				<div class="w-full block">
					<div class="btn --full-w sm:py-6 --outline load-more-cards">Show More</div>
				</div>
			{% endif %}
			{# / End #}

		</section>


		{# Aside #}
		<aside class="aside-content w-full lg:w-1/3 mt-14 lg:mt-0 md:px-4 clearfix">
			<div class="max-w-sm lg:max-w-xs w-full m-auto lg:float-right ">
				<p class="border-grey-lightest border-t leading-normal pt-6 text-xs text-grey-dark">WEAT actively tracks and testifies as appropriate on TCEQ proposals and policies through collaboration between our Executive Director, Julie Nahrgang, WEAT President and leadership, and WEAT Knowledge Committees.</p>
			</div>
		</aside>

	</div>

</div>


{% endblock %}
