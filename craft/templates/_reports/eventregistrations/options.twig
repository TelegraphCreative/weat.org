{% import "_includes/forms" as forms %}
{% set events = craft.venti.events.registeredEvent(1) %}
{% set optionValues = {} %}
{% for event in events %}
  {% set optionValues = optionValues|merge({(event.id): event.title}) %}
{% endfor %}

{#% set optionValues = {
    139 : 139,
    "antartica" : "Antartica",
    "asia" : "Asia",
    "australia" : "Australia",
    "europe" : "Europe",
    "northAmerica" : "North America",
    "southAmerica" : "South America"
}
%#}

{{ forms.selectField({
    label: "Event"|t,
    name: "event",
    options: optionValues,
    value: options.event is defined ? options.event : "Antartica"
}) }}
